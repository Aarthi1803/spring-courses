spring.application.name=SpringOAuth-ChatGPT

spring.security.oauth2.client.registration.github.client-id=Ov23liHR1pM2R2WvfeVE
spring.security.oauth2.client.registration.github.client-secret=74cfe706e4e413a94c80d78905d5421240751e6f
spring.security.oauth2.client.registration.github.scope=user,repo,read:user
spring.security.oauth2.client.registration.github.redirect-uri=http://localhost:8080/login/oauth2/code/github

# Scope user,repo --> Requests access to user profile & repositories.
# Redirect URI --> Must match GitHub's OAuth settings. This tells GitHub where to send the user after login.
# scope=read:user --> This ensures you get full profile details, including name.

# Scopes define what permissions are granted.
# user - Fetch basic user details.
# repo - (Optional) Access private/public repositories.
# read:user - Allows reading full user profile details.